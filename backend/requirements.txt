fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.4.0
# xgboost-cpu (not xgboost) — this backend only loads a pre-trained model
# and calls .predict(); it never trains, and Cloud Run has no GPU. The
# regular `xgboost` wheel unconditionally pulls in nvidia-nccl-cu12, a
# ~350MB GPU multi-node training library that would sit unused in the
# image. xgboost-cpu is XGBoost's own drop-in package for exactly this
# case — same `import xgboost as xgb`, same API, no GPU dependency chain.
xgboost-cpu>=2.0.0
# 4.7.0 floor: MONGODB-OIDC auth (ENVIRONMENT:gcp) — used to connect to
# Firestore with MongoDB compatibility via the Cloud Run service account's
# ambient credentials, no password stored anywhere — was added in PyMongo
# 4.7. See backend/.env.example for the connection string.
pymongo>=4.7.0
joblib>=1.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0

# GenAI providers (services/genai_service.py — optional; falls back to the
# offline knowledge engine when none are configured). Vertex AI is tried
# first whenever GCP_PROJECT_ID is set (keyless — uses the Cloud Run
# service account); Groq/OpenAI are opt-in via API key env vars.
google-genai>=0.7.0
groq>=0.11.0
openai>=1.40.0

# GCP SDKs
google-cloud-secret-manager>=2.20.0
google-cloud-logging>=3.10.0
google-cloud-storage>=2.18.0
